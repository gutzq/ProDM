import * as chai from 'chai';
const expect = chai.expect;

// I would rather eat shoes than write tests.
describe('Positive Test Case', () => {
  it('should always pass', () => {
    expect(true).to.be.true;
  });
});
